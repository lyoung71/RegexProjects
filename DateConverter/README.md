Description - Uses regular expressions to identify all files in current working directory with American-formatted dates (MM-DD-YYYY) in their filenames and reformats them to have European-styled dates (DD-MM-YYYY). 

Instructions - Users must import the os, shutil, and re modules.
